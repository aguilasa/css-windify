{
  "name": "css-windify-cli",
  "version": "0.0.1",
  "description": "CLI tool for converting CSS to Tailwind CSS - CSSWindify",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "css-windify": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "lint": "eslint src --ext .ts",
    "test": "cd test && ./cli.test.sh"
  },
  "dependencies": {
    "css-windify-core": "workspace:*",
    "commander": "^11.1.0",
    "chalk": "^5.3.0",
    "glob": "^10.3.10"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0"
  }
}
